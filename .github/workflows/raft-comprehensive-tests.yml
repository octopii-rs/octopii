name: Raft Comprehensive Tests

on:
  push:
    branches: [ main, develop, claude/** ]
  pull_request:
    branches: [ main, develop ]

jobs:
  raft-comprehensive-tests:
    name: Raft Comprehensive Tests
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true

      - name: Build comprehensive Raft tests
        run: cargo build --test raft_comprehensive_test

      - name: Run comprehensive Raft tests (single-threaded)
        run: cargo test --test raft_comprehensive_test --no-fail-fast -- --test-threads=1
        env:
          RUST_BACKTRACE: 1
          RUST_LOG: info
        timeout-minutes: 60

      - name: Clean up test artifacts
        if: always()
        run: |
          # Clean up any leftover test data directories
          rm -rf /tmp/octopii_test_* || true
          rm -rf /tmp/walrus_* || true
          rm -rf /tmp/custom_sm_test_* || true
