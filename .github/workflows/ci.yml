name: CI

on:
  push:
  pull_request:

jobs:
  unit-tests:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --lib --no-fail-fast

  transport-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test transport_test --no-fail-fast
      - if: always()
        run: rm -rf /tmp/octopii_test_* /tmp/walrus_* || true

  rpc-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test rpc_test --no-fail-fast
      - if: always()
        run: rm -rf /tmp/octopii_test_* /tmp/walrus_* || true

  chunk-transfer-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test chunk_transfer_test --no-fail-fast
      - if: always()
        run: rm -rf /tmp/chunk_test_* /tmp/octopii_test_* || true

  chunk-error-handling-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test chunk_error_handling_test --no-fail-fast
      - if: always()
        run: rm -rf /tmp/chunk_test_* /tmp/octopii_test_* || true

  chunk-large-transfer-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test chunk_large_transfer_test --no-fail-fast
      - if: always()
        run: rm -rf /tmp/chunk_test_* /tmp/octopii_test_* || true

  chunk-mixed-scenarios-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test chunk_mixed_scenarios_test --no-fail-fast
      - if: always()
        run: rm -rf /tmp/chunk_test_* /tmp/octopii_test_* || true

  chunk-parallel-stress-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test chunk_parallel_stress_test --no-fail-fast
      - if: always()
        run: rm -rf /tmp/chunk_test_* /tmp/octopii_test_* || true

  integration-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test integration_test --no-fail-fast
      - if: always()
        run: rm -rf /tmp/octopii_test_* /tmp/walrus_* || true

  wal-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test wal_test --no-fail-fast
      - if: always()
        run: rm -rf /tmp/test_walrus_* /tmp/walrus_* || true

  raft-cluster-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test raft_cluster_test --no-fail-fast -- --test-threads=1
      - if: always()
        run: rm -rf /tmp/octopii_test_* /tmp/walrus_* /tmp/custom_sm_test_* || true

  raft-durability-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test raft_durability_test --no-fail-fast -- --test-threads=1
      - if: always()
        run: rm -rf /tmp/octopii_test_* /tmp/walrus_* /tmp/custom_sm_test_* || true

  raft-core-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test raft_core_tests --no-fail-fast -- --test-threads=1
      - if: always()
        run: rm -rf /tmp/octopii_test_* /tmp/walrus_* /tmp/custom_sm_test_* || true

  raft-chaos-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test raft_chaos_tests --no-fail-fast -- --test-threads=1
      - if: always()
        run: rm -rf /tmp/octopii_test_* /tmp/walrus_* /tmp/custom_sm_test_* || true

  raft-advanced-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test raft_advanced_tests --no-fail-fast -- --test-threads=1
      - if: always()
        run: rm -rf /tmp/octopii_test_* /tmp/walrus_* /tmp/custom_sm_test_* || true

  raft-consistency-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true
      - run: cargo test --test raft_consistency_tests --no-fail-fast -- --test-threads=1
      - if: always()
        run: rm -rf /tmp/octopii_test_* /tmp/walrus_* /tmp/custom_sm_test_* || true
